#include<stdio.h>
int swap(int*x, int*y);
int main()
{
    int a,b;
    printf("enter two numbers a and b:\n");
    scanf("%d%d",&a,&b);
    printf("a is:%d\nbis:%d\n",a,b);
    swap(&a,&b);
    printf("value of a=%d\nb=%d",a,b);
    return 0;
}
int swap(int*x,int*y)
{ int 
      t=*x;
      *x=*y;
      *y=t;
  printf("new value of a and b are:%d\n%d respectively\n",*x,*y);
    
}
